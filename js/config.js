// Constants and configuration from index.html

const CONFIG = {
    API_URL: 'https://api.example.com',
    MAX_RETRIES: 5,
    TIMEOUT: 30000,
    FEATURE_FLAGS: {
        newFeature: true,
        betaAccess: false
    },
    APP_VERSION: '1.0.0',
};

export default CONFIG;